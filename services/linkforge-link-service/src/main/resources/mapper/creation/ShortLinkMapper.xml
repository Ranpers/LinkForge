<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.github.ranpers.linkforge.link.creation.adapter.out.persistence.ShortLinkMapper">

    <select id="findByIdempotencyKey"
            resultType="io.github.ranpers.linkforge.link.creation.adapter.out.persistence.ShortLinkRow">
        SELECT id,
               created_by_user_id AS createdByUserId,
               group_id AS groupId,
               name,
               link_code AS linkCode,
               code_type AS codeType,
               full_url AS fullUrl,
               sort_order AS sortOrder,
               domain_id AS domainId,
               expires_at AS expiresAt,
               idempotency_key AS idempotencyKey,
               request_fingerprint AS requestFingerprint,
               created_at AS createdAt
        FROM t_link
        WHERE created_by_user_id = #{userId}
          AND idempotency_key = #{idempotencyKey}
    </select>

    <select id="groupBelongsToUser" resultType="boolean">
        SELECT EXISTS(
            SELECT 1
            FROM t_group
            WHERE id = #{groupId}
              AND user_id = #{userId}
              AND deleted_at IS NULL
        )
    </select>

    <insert id="tryInsert">
        INSERT INTO t_link
            (id, created_by_user_id, group_id, name, link_code, code_type, full_url,
             sort_order, domain_id, expires_at, idempotency_key,
             request_fingerprint, created_at, updated_at)
        VALUES
            (#{link.id}, #{link.createdByUserId}, #{link.groupId}, #{link.name},
             #{link.linkCode}, #{link.codeType}, #{link.fullUrl}, #{link.sortOrder},
             #{link.domainId}, #{link.expiresAt}, #{link.idempotencyKey},
             #{link.requestFingerprint}, #{link.createdAt}, #{link.createdAt})
        ON CONFLICT DO NOTHING
    </insert>

    <select id="existsById" resultType="boolean">
        SELECT EXISTS(
            SELECT 1
            FROM t_link
            WHERE id = #{linkId}
        )
    </select>

    <select id="existsByDomainAndCode" resultType="boolean">
        SELECT EXISTS(
            SELECT 1
            FROM t_link
            WHERE domain_id = #{domainId}
              AND link_code = #{linkCode}
        )
    </select>

</mapper>
