server:
  port: 9001

spring:
  application:
    name: linkforge-user-service

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://127.0.0.1:9000

management:
  endpoints:
    web:
      exposure:
        include:
          - health
          - info