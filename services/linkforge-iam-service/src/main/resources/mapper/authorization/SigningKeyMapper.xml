<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.github.ranpers.linkforge.iam.authorization.adapter.out.persistence.SigningKeyMapper">

    <select id="acquireInitializationLock" resultType="boolean">
        SELECT pg_advisory_xact_lock(#{lockId}) IS NULL
    </select>

    <select id="findActive"
            resultType="io.github.ranpers.linkforge.iam.authorization.adapter.out.persistence.SigningKeyDO">
        SELECT key_id AS "keyId",
               algorithm,
               public_key_der AS "publicKeyDer",
               private_key_der AS "protectedPrivateKey"
        FROM t_authorization_jwk
        WHERE status = 1
        ORDER BY created_at DESC
        LIMIT 1
    </select>

    <insert id="insert">
        INSERT INTO t_authorization_jwk (
            key_id,
            algorithm,
            public_key_der,
            private_key_der,
            status
        ) VALUES (
            #{keyId},
            #{algorithm},
            #{publicKeyDer},
            #{protectedPrivateKey},
            1
        )
    </insert>

    <update id="migratePrivateKey">
        UPDATE t_authorization_jwk
        SET private_key_der = #{protectedValue}
        WHERE key_id = #{keyId}
          AND private_key_der = #{expectedValue}
    </update>

</mapper>
